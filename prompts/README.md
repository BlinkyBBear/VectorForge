# Prompt history

This folder records the major product prompts used to design and build VectorForge.
Keep new significant prompts here when iterating so Git history stays self-explanatory.

| File | Purpose |
| --- | --- |
| `001-initial-vectorforge.md` | Original full product brief (raster → laser SVG) |
| `002-desktop-bg-removal-memory.md` | Background removal, memory safety, .exe, GitHub structure |

When you refine the product, add `003-….md` with the full prompt text and a short note at the top (date + goal).
